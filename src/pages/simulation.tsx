import CardSimulator from "@/wrapper/cardSimulator";
import React from "react";

const Simulation = () => {
  return (
    <div>        
      <CardSimulator />
    </div>
  );
};

export default React.memo(Simulation);
