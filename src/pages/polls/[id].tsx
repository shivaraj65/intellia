import PollsScreen from "@/screens/pollsScreen";
import React from "react";

const Polls = () => {
    return(
        <div>
            <PollsScreen/>
        </div>
    )
}

export default React.memo(Polls);